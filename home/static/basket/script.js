

let delete_button = document.getElementById('#modal_button');

delete_button.addEventListener('click', function(e) {
    delete_button.style.opacity = 1;
    delete_button.style.filter = 'blur(0)';
    delete_button.style.transform = 'translateX(0)';
})


delete_button.addEventListener('checked', function(e) {

})
